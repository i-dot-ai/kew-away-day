Build a simple Cabinet Secretary objectives dashboard as a single HTML file.

Use the objectives data from `data/OBJECTIVES.md` as the data source. The dashboard should:

- Display all 5 objectives and their associated actions as a clean, readable page
- Use the GOV.UK Design System colour palette and styling as described in CLAUDE.md
- Include a header with "Cabinet Secretary Objectives 2026–2027" and Dame Antonia Romeo's name
- List each objective as a numbered section with its actions as bullet points underneath
- Keep it simple — no charts, no interactivity, no JavaScript. Just well-styled HTML and CSS.
- Open automatically in the browser when complete

Put the project in `projects/cab-sec-dashboard/`.

Keep the output minimal — this is a starting point that the user will improve with follow-up prompts.
